interface:
  display_name: "GEO Response Analyzer"
  short_description: "Analyze market-proxy GEO visibility"
  default_prompt: "Use $geo-response-analyzer to analyze cleaned ChatGPT GEO responses for market-proxy visibility, weighted share of voice, average rank, recommendation quality, sentiment, topic gaps, and risk signals."

policy:
  allow_implicit_invocation: true
