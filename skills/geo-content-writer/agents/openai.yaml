interface:
  display_name: "GEO Content Writer"
  short_description: "Write market-proxy GEO content"
  default_prompt: "Use $geo-content-writer to turn a market-proxy GEO report content brief into buyer-topic Markdown drafts, a content plan, and a QA report."

policy:
  allow_implicit_invocation: true
