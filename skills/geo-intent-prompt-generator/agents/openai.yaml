interface:
  display_name: "GEO Intent Prompt Generator"
  short_description: "Generate market-proxy GEO prompts"
  default_prompt: "Use $geo-intent-prompt-generator to create a market-proxy AI visibility prompt set from my services, personas, target market, language, platform, customer questions, pain points, competitors, and optional website domain."

policy:
  allow_implicit_invocation: true
