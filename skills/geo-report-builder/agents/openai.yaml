interface:
  display_name: "GEO Report Builder"
  short_description: "Build GEO diagnostic reports"
  default_prompt: "Use $geo-report-builder to turn GEO response analysis outputs into a client-facing diagnostic report and content-writer brief."

policy:
  allow_implicit_invocation: true
